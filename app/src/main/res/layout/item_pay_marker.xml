<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/marker_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:background="@drawable/bg_item_pay_marker"
        android:paddingHorizontal="@dimen/marker_padding_horizontal">

        <TextView
            android:id="@+id/marker_type_text_view"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:background="@drawable/bg_free_marker_inside"
            android:text="@string/pay"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            android:gravity="center"
            android:textColor="@color/markerIconTextColor"
            android:textSize="@dimen/marker_icon_text_size"/>

        <TextView
            android:id="@+id/fee_text_view"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:text="3,000"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:gravity="center"
            android:layout_marginLeft="@dimen/marker_margin_left"
            android:textColor="@color/markerFeeTextColor"
            android:textSize="@dimen/marker_fee_text_size"
            app:layout_constraintLeft_toRightOf="@+id/marker_type_text_view"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>